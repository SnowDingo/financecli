[project]
name = "fincli"
version = "0.1.0"
description = "A free finance cli app that handles day to day finance tasks and operations"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytz>=2026.3.post1",
    "requests>=2.34.2",
    "typer>=0.27.1",
]
